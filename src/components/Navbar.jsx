import { AppBar, Toolbar } from '@material-ui/core'
import React from 'react'

export default function Navbar() {
    return (
        <AppBar>
            <Toolbar>
            </Toolbar>
        </AppBar>
    )
}
